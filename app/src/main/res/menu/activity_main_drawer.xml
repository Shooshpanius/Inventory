<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_offline_create"
            android:icon="@drawable/ic_menu_edit"
            android:title="Offline (create)" />
        <item
            android:id="@+id/nav_offline_inventory"
            android:icon="@drawable/ic_menu_edit"
            android:title="Offline (inventory)" />
        <item
            android:id="@+id/nav_online_create"
            android:icon="@drawable/ic_menu_edit"
            android:title="Online (create)" />
        <item
            android:id="@+id/nav_online_inventory"
            android:icon="@drawable/ic_menu_edit"
            android:title="Online (Inventory)" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_manage"
                android:icon="@drawable/ic_menu_manage"
                android:title="Settings" />
            <item
                android:id="@+id/nav_details"
                android:icon="@drawable/ic_menu_info_details"
                android:title="Info" />
        </menu>
    </item>

</menu>
